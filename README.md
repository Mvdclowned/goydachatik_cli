# GoydaChatik: CLI

![Go Version](https://img.shields.io/badge/Go-1.20+-00ADD8?style=flat&logo=go)
![License](https://img.shields.io/badge/License-MIT-green)
![Security](https://img.shields.io/badge/Security-End--to--End-red)

**GoydaChatik** — это минималистичный CLI-мессенджер с настоящим **End-to-End (E2E)** шифрованием. 

Никаких баз данных. Никаких логов. Никакого браузера. Только вы, терминал и собеседник.

---

## ✨ Возможности

* **Настоящее E2E Шифрование:** Сообщения шифруются на вашем устройстве перед отправкой. Сервер видит только зашифрованный мусор.
* **Скорость:** Работает мгновенно благодаря протоколу WebSocket.
* **Анонимность:** Сервер не хранит логи (все держится в RAM). После перезагрузки сервера история исчезает навсегда.
* **Кроссплатформенность:** Работает на Linux, macOS и Windows.
* **Heartbeat:** Автоматическая поддержка соединения (защита от разрывов).

---

## 🛡️ Техническое обоснование безопасности

Этот проект использует криптографию промышленного стандарта. Вы можете проверить код в `client/main.go`.

### 1. Обмен ключами (ECDH)
При запуске клиента генерируется **эфемерная пара ключей** (Private/Public) на эллиптической кривой **P-256**.
* Клиенты обмениваются **Публичными ключами** через сервер.
* Приватный ключ **никогда** не покидает оперативную память вашего компьютера.

### 2. Шифрование сообщений (AES-GCM)
Для каждого сообщения используется алгоритм **AES-256** в режиме **GCM** (Galois/Counter Mode).
* Это обеспечивает и конфиденциальность, и целостность данных.
* Для каждого сообщения генерируется уникальный `Nonce`.

### 3. Роль Сервера
Сервер (`server/main.go`) выступает исключительно как **ретранслятор**. Он не выполняет никаких криптографических операций и не имеет ключей для расшифровки поля `Content`.

---

## 🛠️ Установка и Запуск

Вам понадобится установленный [Go](https://go.dev/dl/).

### Шаг 1: Скачать репозиторий
```bash
git clone https://github.com/Mvdclowned/goydachatik_cli.git
cd goydachatik_cli
```
### Шаг 2: Запустить Сервер (Матрица)
Сервер пересылает зашифрованные пакеты.
```bash
cd server
```
```go
go run main.go
```
> *По умолчанию сервер слушает порт :8080*
## Шаг 3: Запустить Клиент (Оператор)
Откройте новое окно терминала.
```bash
cd client
```
```go
go run main.go
```
* **Введите юзернейм.**

* **Введите номер комнаты.**

* **Ждите собеседника! Шифрование активируется, как только второй человек зайдет в ту же комнату.**

### 🌐 Работа через Интернет (WAN)
* **Чтобы общаться с другом из другого города, сервер должен быть доступен извне. Самый простой способ — использовать Ngrok.**

* **Запустите сервер локально:**
```go
   go run main.go
```
Для этого утсновите [Ngrok](https://ngrok.com/download/)

* **Запустите Ngrok:**
```bash
ngrok http 8080
```
Скопируйте ссылку вида https://xxxx.ngrok-free.app.

* **Друг запускает клиент так:**
```go
go run main.go -server="xxxx.ngrok-free.app"
```
